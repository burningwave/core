name: Build, test & check coverage 

on:
  watch:
    types: [started]

jobs:
  authorize: 
    runs-on: ubuntu-latest

    steps:
    - uses: octokit/request-action@v2.0.0
      with:
        route: GET /repos/:repository/collaborators/${{ github.actor }}
        repository: ${{ github.repository }}
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    
  build-and-test-with-Java-8:
    needs: [authorize]
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 8
      uses: actions/setup-java@v1
      with:
        java-version: 1.8
        java-package: jdk  
    - name: Build & test
      run: mvn -B clean compile -DskipTests=false -DjavadocExecutable.relativePath=../bin/javadoc --file pom.xml

  build-and-test-with-Java-9:
    needs: [authorize]
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 9
      uses: actions/setup-java@v1
      with:
        java-version: 9
        java-package: jdk  
    - name: Build & test
      run: mvn -B clean compile -Dproject.jdk.version=9 -DskipTests=false --file pom.xml

  build-and-test-with-Java-10:
    needs: [authorize]
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 10
      uses: actions/setup-java@v1
      with:
        java-version: 10
        java-package: jdk  
    - name: Build & test
      run: mvn -B clean compile -Dproject.jdk.version=10 -DskipTests=false --file pom.xml
  
  build-and-test-with-Java-11:
    needs: [authorize]
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 11
      uses: actions/setup-java@v1
      with:
        java-version: 11
        java-package: jdk  
    - name: Build & test
      run: mvn -B clean compile -Dproject.jdk.version=11 -DskipTests=false --file pom.xml
  
  build-and-test-with-Java-12:
    needs: [authorize]
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 12
      uses: actions/setup-java@v1
      with:
        java-version: 12
        java-package: jdk  
    - name: Build & test
      run: mvn -B clean compile -Dproject.jdk.version=12 -DskipTests=false --file pom.xml
  
  build-and-test-with-Java-13:
    needs: [authorize]
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 13
      uses: actions/setup-java@v1
      with:
        java-version: 13
        java-package: jdk  
    - name: Build & test
      run: mvn -B clean compile -Dproject.jdk.version=13 -DskipTests=false --file pom.xml
  
  build-and-test-with-Java-14:
    needs: [authorize]
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 14
      uses: actions/setup-java@v1
      with:
        java-version: 14
        java-package: jdk  
    - name: Build & test
      run: mvn -B clean compile -Dproject.jdk.version=14 -DskipTests=false --file pom.xml
 
  coverage:
    name: Coverage
    runs-on: ubuntu-latest
    needs: [authorize, build-and-test-with-Java-8, build-and-test-with-Java-9, build-and-test-with-Java-10, build-and-test-with-Java-11, build-and-test-with-Java-12, build-and-test-with-Java-13, build-and-test-with-Java-14]
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-java@v1
        with:
          java-version: 8
          architecture: x64
      - name: Build project
        run: mvn clean test -DskipTests=false -P run-coveralls jacoco:report coveralls:report -DrepoToken=${{ secrets.coveralls_repo_token }}
